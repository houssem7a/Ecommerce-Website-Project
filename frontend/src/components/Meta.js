import React from "react";
import { Helmet } from "react-helmet";

const Meta = ({ title, description, keywords }) => {
  return (
    <Helmet>
      <title>{title}</title>
      <meta name="description" content={description} />
      <meta name="keyword" content={keywords} />
    </Helmet>
  );
};

Meta.defaultProps = {
  title: "BIKE SHOP",
  description: "We sell the best bikes for cheap",
  keywords: "electronics, mechanical, cheap bikes",
};

export default Meta;
